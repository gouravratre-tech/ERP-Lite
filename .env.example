# Copy this file's values into Render's "Environment Variables" section
# (or export them in your shell for local testing). Never commit real
# credentials to Git — this file is just a template.

DATABASE_URL=postgresql://user:password@ep-something.neon.tech/neondb?sslmode=require
APP_USERNAME=choose-a-username
APP_PASSWORD=choose-a-strong-password
